This directory contains mostly legacy regression test scripts that will soon no
longer be used. The new way to check for regressions is via the following two
scripts:

  generator.py  - to generate regression tests and
  tester.py - to check for regressions

So tester.py is sufficient.
